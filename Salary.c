#include <stdio.h>
int main()
{
    int NUMBER,a;
    float b,k;
    scanf("%d %d %f",&NUMBER,&a,&b);
    k = (a*b);
    printf("NUMBER = %d\n",NUMBER);
    printf("SALARY = U$ %0.2f\n",k);
    return 0;

}
