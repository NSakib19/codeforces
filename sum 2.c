#include<stdio.h>
int main()
{
    int n,m;
    scanf("%d %d",&n,&m);
    if(n==m)
        {printf("The numbers are equal\n");
        }
        else{printf("The numbers aren't equal\n");}
        return 0;
}
